import 'package:flutter/material.dart';

class SearchSongsAppbar extends StatelessWidget {
  const SearchSongsAppbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
